# Base URL del API Gateway. En el navegador NUNCA se usa 10.0.2.2 (eso es solo
# el alias del emulador Android) — en desarrollo local normal es localhost.
VITE_API_BASE_URL=https://meditrack-gateway.onrender.com
